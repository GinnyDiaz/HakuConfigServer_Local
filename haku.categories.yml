#HTTP Server
server:
  port: ${PORT:7000}

#Discovery Server Access
eureka:
  client:
    service-url:
      default-zone: http://localhost:5100/eureka
  instance:
    status-page-url-path: /infoCategories
    health-check-url-path: /healthCategories
    metadata-map:
       app-owner: Haku  
       description: Microservice for Categories management
