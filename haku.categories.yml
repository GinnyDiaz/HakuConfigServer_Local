#HTTP Server
server:
  port: ${PORT:7000}

#Discovery Server Access
eureka:
  client:
    service-url:
      default-zone: http://localhost:5000/eureka
  instance:
    status-page-url-path: /infoCatalogo
    health-check-url-path: /healthCatalogo
    metadata-map:
       app-owner: Haku  
       description: test application for the Haku Server
